kind: Deployment
apiVersion: apps/v1
metadata:
  name: smart-home-webui
  namespace: smart-home
  labels:
    k8s-app: smart-home-webui
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: smart-home-webui
  template:
    metadata:
      name: smart-home-webui
      labels:
        k8s-app: smart-home-webui
    spec:
      containers:
        - name: smart-home-webui
          image: 'localhost:5000/smarthome-webui:0.8'
          resources:
              limits:
                memory: 512Mi
                cpu: 1
          securityContext:
            privileged: false
          envFrom:
            - configMapRef:
                name: smarthome-config
      initContainers:
        - name: init-frontend
          image: busybox
          command: ['sh', '-c', 'until nslookup smart-home-db; do echo waiting for smart-home-db; sleep 2; done;']
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
---
kind: Service
apiVersion: v1
metadata:
  name: smart-home-webui
  namespace: smart-home
  labels:
    k8s-app: smart-home-webui
spec:
  ports:
    - protocol: TCP
      port: 3000
  selector:
    k8s-app: smart-home-webui
---
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: smarthome-webui
  namespace: smart-home
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/app-root: /
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    #- host: cx
     - http:
        paths:
          - path: /ui(.*)
            backend:
              serviceName: smart-home-webui
              servicePort: 3000